import { styled } from '../../styles/stitched'

export const Canvas = styled('canvas', {
  height: '60px',
  width: '100%',
  display: 'block',
  backgroundColor: '$inputBorder',
  borderRadius: '$input',
})
